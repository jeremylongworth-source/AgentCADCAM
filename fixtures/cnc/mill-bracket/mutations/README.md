# CNC Mill Bracket Mutations

Mutation descriptors transform a copy of `programs/positive.nc`. They must never modify the positive program in place. Each mutation has an expected hard blocker in `../expected/outcomes.yaml`.

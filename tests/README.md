# Tests

Foundation tests validate repository contracts, JSON schema syntax, YAML manifests, source metadata requirements, broken references, and hard safety invariants. Later phases add workflow-specific golden fixtures and practitioner evaluation packets.
